# План перехода к новой структуре БД — Модуль склада QAZRem

> Дата: 25.06.2026
> Версия: 2.0 (упрощённая)
> Статус: ✅ РЕАЛИЗОВАНО — все 4 этапа
> Основание: ТЗ-склад.md (все 12 блоков согласованы)
> Принцип: минимум новых таблиц, логика в коде, не в схеме.

---

## 📊 Текущая схема → Целевая схема

### Что есть сейчас:

| Таблица | Ключевые поля |
|---------|---------------|
| `parts` | id, name, sku, compatible_models(JSONB), purchase_price, selling_price, quantity, min_quantity |
| `part_movements` | id, part_id, type(incoming/outgoing/writeoff), quantity, order_id, document, supplier_id, supplier_sku, batch_number |
| `order_parts` | id, order_id, part_id, quantity_used, purchase_price_at_moment, selling_price_at_moment |
| `suppliers` | id, name, contact_person, phone, email, notes |
| `locations` | id, name, address |
| `device_catalog` | id, brand, model, group_name |

### Что добавляем (8 таблиц вместо 11):

| Новая таблица | Назначение | Блок ТЗ |
|---------------|------------|---------|
| `part_categories` | Иерархия: Категория → Подкатегория (parent_id) | Блок 1 |
| `category_attributes` | Шаблон динамических атрибутов (какие поля у категории). **Значения — JSONB в parts** | Блок 4 |
| `tags` | Справочник тегов | Блок 5 |
| `part_tags` | Связь M:N запчасти ↔ теги | Блок 5 |
| `part_batches` | Партии для FIFO | Блок 7 |
| `reservations` | Резервирование под заказ | Блок 6 |
| `inventory_sheets` + `inventory_items` | Инвентаризация | Блок 6 |
| `equipment` | Оборудование мастеров | Блок 6 |

> ❌ **Убрано из v1 плана:**
> - `part_attribute_values` — заменено на `parts.attributes JSONB`
> - `supplier_returns` — для v1 это просто движение с типом `return_supplier`
> - Поле `subcategory_id` в parts — глубина через `parent_id`
> - Поле `supplier_warranty_months` в parts — только в batches
> - Вычисляемое `warranty_expires_at` — считается на лету
> - Типы движений `reserve`/`unreserve`/`transfer_in`/`transfer_out` — избыточно

---

## 🗺️ 4 этапа миграции

### ✅ Этап 0 — Подготовка (ручной)

- [x] Бэкап БД
- [x] Проверить уникальность SKU в `parts`
- [x] Зафиксировать текущие цены для истории

---

### ✅ Этап 1 — Фундамент: категории + теги + атрибуты

**Файл миграции:** `1720000000000_warehouse-foundation.sql` ✅

```sql
-- ============================================================
-- 1.1 Категории (иерархия через parent_id)
-- ============================================================
CREATE TABLE part_categories (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  parent_id INT REFERENCES part_categories(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(name, parent_id)
);

-- ============================================================
-- 1.2 Шаблоны атрибутов для категорий
-- ============================================================
CREATE TABLE category_attributes (
  id SERIAL PRIMARY KEY,
  category_id INT NOT NULL REFERENCES part_categories(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  attr_type VARCHAR(20) NOT NULL DEFAULT 'string',  -- string/number/boolean/select
  attr_options JSONB,            -- для select: ["OLED","LCD","TFT"]
  sort_order INT NOT NULL DEFAULT 0,
  is_required BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(category_id, name)
);

-- ============================================================
-- 1.3 Теги
-- ============================================================
CREATE TABLE tags (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL UNIQUE,
  color VARCHAR(7) DEFAULT '#6b7280',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE part_tags (
  part_id INT NOT NULL REFERENCES parts(id) ON DELETE CASCADE,
  tag_id INT NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
  PRIMARY KEY (part_id, tag_id)
);

-- ============================================================
-- 1.4 Расширяем parts
-- ============================================================
ALTER TABLE parts
  ADD COLUMN category_id INT REFERENCES part_categories(id),
  ADD COLUMN model_name VARCHAR(255),
  ADD COLUMN attributes JSONB NOT NULL DEFAULT '{}',
  ADD COLUMN unit VARCHAR(20) NOT NULL DEFAULT 'шт',
  ADD COLUMN photo_url TEXT,
  ADD COLUMN is_active BOOLEAN NOT NULL DEFAULT TRUE;

-- ============================================================
-- 1.5 Категория-заглушка для существующих запчастей
-- ============================================================
INSERT INTO part_categories (id, name) VALUES (0, 'Без категории')
ON CONFLICT DO NOTHING;
UPDATE parts SET category_id = 0 WHERE category_id IS NULL;
```

**Что меняется в коде:**
- `routes/parts.ts` — добавить category_id, tags, attributes в CRUD
- Новый `routes/warehouse/categories.ts` — CRUD категорий + атрибутов-шаблонов

---

### ✅ Этап 2 — Партии и FIFO

**Файл миграции:** `1720010000000_warehouse-batches.sql` ✅

```sql
-- ============================================================
-- 2.1 Партии
-- ============================================================
CREATE TABLE part_batches (
  id SERIAL PRIMARY KEY,
  part_id INT NOT NULL REFERENCES parts(id),
  batch_number VARCHAR(100) NOT NULL,
  supplier_id INT NOT NULL REFERENCES suppliers(id),
  purchase_price DECIMAL(10,2) NOT NULL,
  initial_quantity INT NOT NULL CHECK (initial_quantity > 0),
  current_quantity INT NOT NULL CHECK (current_quantity >= 0),
  received_at DATE NOT NULL DEFAULT CURRENT_DATE,
  supplier_warranty_months INT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(part_id, batch_number)
);
-- warranty_expires_at = received_at + supplier_warranty_months (вычисляется в SELECT)

-- ============================================================
-- 2.2 Расширяем part_movements
-- ============================================================
ALTER TABLE part_movements
  ADD COLUMN batch_id INT REFERENCES part_batches(id),
  ADD COLUMN location_id INT REFERENCES locations(id);

-- ============================================================
-- 2.3 Новые типы движений (6 вместо 10)
-- ============================================================
ALTER TABLE part_movements DROP CONSTRAINT part_movements_type_check;
ALTER TABLE part_movements ADD CONSTRAINT part_movements_type_check
  CHECK (type IN (
    'incoming',        -- оприходование
    'outgoing',        -- списание на заказ
    'writeoff',        -- списание брак/потеря
    'return_order',    -- возврат с заказа
    'return_supplier', -- возврат поставщику
    'correction'       -- корректировка
  ));

-- ============================================================
-- 2.4 batch_id в order_parts
-- ============================================================
ALTER TABLE order_parts
  ADD COLUMN batch_id INT REFERENCES part_batches(id);

-- ============================================================
-- 2.5 Партия-заглушка для существующих остатков
-- ============================================================
-- Для каждой запчасти с quantity > 0 создаём одну партию
INSERT INTO part_batches (part_id, batch_number, supplier_id, purchase_price,
                           initial_quantity, current_quantity, received_at)
SELECT
  p.id,
  'LEGACY-' || p.id,
  (SELECT id FROM suppliers ORDER BY id LIMIT 1),
  p.purchase_price,
  p.quantity,
  p.quantity,
  NOW()
FROM parts p
WHERE p.quantity > 0
  AND NOT EXISTS (SELECT 1 FROM part_batches pb WHERE pb.part_id = p.id);
```

**Что меняется в коде:**
- `routes/parts.ts` — оприходование создаёт партию; списание — FIFO из `part_batches`
- `routes/orders.ts` — списание на заказ через FIFO, возврат в ту же партию
- `routes/suppliers.ts` — возврат поставщику = movement `return_supplier` + уменьшение `current_quantity` в партии

---

### ✅ Этап 3 — Операции: резервы, инвентаризация, оборудование

**Файл миграции:** `1720020000000_warehouse-operations.sql` ✅

```sql
-- ============================================================
-- 3.1 Резервирование
-- ============================================================
CREATE TABLE reservations (
  id SERIAL PRIMARY KEY,
  part_id INT NOT NULL REFERENCES parts(id),
  batch_id INT REFERENCES part_batches(id),
  order_id INT NOT NULL REFERENCES orders(id),
  quantity INT NOT NULL CHECK (quantity > 0),
  reserved_by INT NOT NULL REFERENCES users(id),
  reserved_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  expires_at TIMESTAMPTZ,
  status VARCHAR(20) NOT NULL DEFAULT 'active'
    CHECK (status IN ('active', 'used', 'cancelled'))
);

-- ============================================================
-- 3.2 Инвентаризация
-- ============================================================
CREATE TABLE inventory_sheets (
  id SERIAL PRIMARY KEY,
  location_id INT REFERENCES locations(id),
  created_by INT NOT NULL REFERENCES users(id),
  status VARCHAR(20) NOT NULL DEFAULT 'draft'
    CHECK (status IN ('draft', 'in_progress', 'completed', 'cancelled')),
  notes TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  completed_at TIMESTAMPTZ
);

CREATE TABLE inventory_items (
  id SERIAL PRIMARY KEY,
  sheet_id INT NOT NULL REFERENCES inventory_sheets(id) ON DELETE CASCADE,
  part_id INT NOT NULL REFERENCES parts(id),
  expected_quantity INT NOT NULL,
  actual_quantity INT,
  notes TEXT
);
-- discrepancy = COALESCE(actual_quantity, expected_quantity) - expected_quantity (в SELECT)

-- ============================================================
-- 3.3 Оборудование мастеров
-- ============================================================
CREATE TABLE equipment (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  master_id INT NOT NULL REFERENCES users(id),
  quantity INT NOT NULL DEFAULT 1,
  notes TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**Что меняется в коде:**
- Новый `routes/warehouse/inventory.ts` — инвентаризация + оборудование
- `routes/orders.ts` — резервирование при добавлении запчасти в заказ

---

### ✅ Этап 4 — Индексы + отчёты

**Файл миграции:** `1720030000000_warehouse-indexes.sql` ✅

```sql
-- Поиск
CREATE INDEX idx_parts_category ON parts(category_id);
CREATE INDEX idx_part_tags_tag ON part_tags(tag_id);
CREATE INDEX idx_part_tags_part ON part_tags(part_id);

-- FIFO: быстрый поиск старейшей партии с остатком
CREATE INDEX idx_batches_fifo ON part_batches(part_id, received_at)
  WHERE current_quantity > 0;

-- Сквозной поиск (trigram)
CREATE INDEX idx_parts_name_trgm ON parts USING gin (name gin_trgm_ops);
CREATE INDEX idx_parts_sku_trgm ON parts USING gin (sku gin_trgm_ops);

-- JSONB-индекс для поиска по атрибутам
CREATE INDEX idx_parts_attributes ON parts USING gin (attributes);

-- Инвентаризация
CREATE INDEX idx_inventory_sheets_status ON inventory_sheets(status);

-- Резервы
CREATE INDEX idx_reservations_order ON reservations(order_id);
CREATE INDEX idx_reservations_status ON reservations(status);
```

**Что меняется в коде:**
- Новый `routes/warehouse/reports.ts` — все 7 отчётов (Блок 12)

---

## 🔄 Порядок выполнения

| Этап | Содержание | Риски | Статус |
|:--:|-----------|:---:|:--:|
| **0** | Бэкап + аудит | 🟢 | ✅ |
| **1** | Категории, теги, атрибуты (JSONB), расширение parts | 🟡 | ✅ |
| **2** | Партии, FIFO, 6 типов движений | 🔴 | ✅ |
| **3** | Резервы, инвентаризация, оборудование | 🟢 | ✅ |
| **4** | Индексы, отчёты | 🟢 | ✅ |

---

## 📁 Структура роутов (упрощённая)

```
backend/src/routes/
  parts.ts                ← расширяется (категории, теги, атрибуты, партии)
  orders.ts               ← расширяется (FIFO-списание, резервы)
  suppliers.ts            ← расширяется (возврат поставщику)
  warehouse/              ← НОВАЯ (3 файла вместо 9)
    categories.ts         ← CRUD категорий + шаблонов атрибутов
    inventory.ts          ← инвентаризация + оборудование мастеров
    reports.ts            ← 7 складских отчётов
```

---

## ⚠️ Ключевые риски

| # | Риск | Решение |
|---|------|---------|
| 1 | **FIFO:** текущий `quantity` — общий котёл | При миграции все остатки → одна партия-заглушка `LEGACY-{part_id}` |
| 2 | **order_parts** без партии | `batch_id` в `order_parts` — опциональный (NULL для старых записей) |
| 3 | **JSONB-атрибуты** vs EAV | Для масштаба мастерской JSONB достаточно. Индекс GIN покрывает поиск. |
| 4 | **SKU-генерация** | Числовой 13-значный, авто + ручная правка, уникальность — constraint |

---

## 📊 Итоговая схема БД

```
┌──────────────────────────────────────────────────────────┐
│                     СКЛАД QAZRem v2                       │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  part_categories ──┬── part_categories (parent_id)        │
│                    │                                      │
│  category_attributes (шаблон)                             │
│                                                           │
│  suppliers ── part_batches ── part_movements              │
│       │           │              │                        │
│       │      parts ─────────────┤                        │
│       │        │   │            │                        │
│       │  part_tags │       order_parts                   │
│       │      │     │            │                        │
│       │    tags    │         orders                       │
│       │            │            │                        │
│       │    attributes(JSONB)  reservations               │
│                                                           │
│  equipment ── users (masters)                             │
│                                                           │
│  inventory_sheets ── inventory_items ── parts             │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

> 💡 **Главный принцип v2:** логика в коде, не в схеме. Меньше таблиц — меньше JOINов — меньше шансов сломать.

---

## 📋 Реализовано (25.06.2026)

### Миграции:
- ✅ `1720000000000_warehouse-foundation.sql` — категории, теги, атрибуты
- ✅ `1720010000000_warehouse-batches.sql` — партии, FIFO, 6 типов движений
- ✅ `1720020000000_warehouse-operations.sql` — резервы, инвентаризация, оборудование
- ✅ `1720030000000_warehouse-indexes.sql` — индексы

### Новые роуты:
- ✅ `routes/warehouse/categories.ts` — CRUD категорий + шаблонов атрибутов
- ✅ `routes/warehouse/inventory.ts` — инвентаризация + оборудование мастеров
- ✅ `routes/warehouse/reports.ts` — 7 складских отчётов

### Изменённые файлы:
- ✅ `routes/parts.ts` — category_id, tags, attributes JSONB, FIFO
- ✅ `app.ts` — регистрация 3 новых роутов

